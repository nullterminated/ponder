{
    attributes = (
        {columnName = answer; name = answer; prototypeName = varchar100; width = 128; }, 
        {
            allowsNull = N; 
            columnName = challengeQuestionID; 
            name = challengeQuestionID; 
            prototypeName = id; 
        }, 
        {allowsNull = N; name = id; prototypeName = id; }, 
        {columnName = salt; name = salt; prototypeName = varchar255; width = 174; }, 
        {allowsNull = N; columnName = userID; name = userID; prototypeName = id; }
    ); 
    attributesUsedForLocking = (answer, challengeQuestionID, id, salt, userID); 
    className = "er.users.model.ERChallengeResponse"; 
    classProperties = (answer, challengeQuestion, salt, user); 
    externalName = ERChallengeResponse; 
    fetchSpecificationDictionary = {}; 
    name = ERChallengeResponse; 
    primaryKeyAttributes = (id); 
    relationships = (
        {
            destination = ERChallengeQuestion; 
            isMandatory = Y; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = id; sourceAttribute = challengeQuestionID; }); 
            name = challengeQuestion; 
        }, 
        {
            destination = ERUser; 
            isMandatory = Y; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = id; sourceAttribute = userID; }); 
            name = user; 
        }
    ); 
}
