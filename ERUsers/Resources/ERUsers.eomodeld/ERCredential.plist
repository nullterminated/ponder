{
    attributes = (
        {columnName = dateCreated; name = dateCreated; prototypeName = jodaDateTime; }, 
        {allowsNull = N; name = id; prototypeName = id; }, 
        {
            columnName = password; 
            name = password; 
            prototypeName = varchar100; 
            width = 128; 
        }, 
        {columnName = salt; name = salt; prototypeName = varchar255; width = 174; }, 
        {allowsNull = N; columnName = userID; name = userID; prototypeName = id; }
    ); 
    attributesUsedForLocking = (id, password, salt, userID); 
    className = "er.users.model.ERCredential"; 
    classProperties = (dateCreated, password, salt, user); 
    externalName = ERCredential; 
    fetchSpecificationDictionary = {}; 
    name = ERCredential; 
    primaryKeyAttributes = (id); 
    relationships = (
        {
            destination = ERUser; 
            isMandatory = Y; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = id; sourceAttribute = userID; }); 
            name = user; 
        }
    ); 
}
